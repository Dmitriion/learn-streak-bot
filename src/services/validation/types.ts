
// Re-export shared types from auth/types.ts to maintain backward compatibility
export type {
  TelegramUser,
  TelegramInitData,
  ValidationResult,
  UserValidationResult,
  InitDataValidationResult
} from '../auth/types';
